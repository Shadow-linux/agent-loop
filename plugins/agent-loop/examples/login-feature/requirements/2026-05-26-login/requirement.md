# Requirement: Login

Existing users need to log in with email and password.

Acceptance:

- Valid credentials create an authenticated session.
- Invalid credentials do not create a session.
- Invalid credentials show a clear error.

Out of scope:

- Registration.
- Password reset.
- Social login.
