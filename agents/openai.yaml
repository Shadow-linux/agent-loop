interface:
  display_name: "Agent Loop"
  short_description: "Single-person agent workflow loop"
  brand_color: "#2563EB"
  default_prompt: "Use $agent-loop to continue this project from the current workflow state."

policy:
  allow_implicit_invocation: true
