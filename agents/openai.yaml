interface:
  display_name: "Agent Loop"
  short_description: "Agent-guided single-human development loop from goal intake to verified close"
  brand_color: "#2563EB"
  default_prompt: "Use TDD for initial Feature and explicit Bug execution. During a bounded Review repair or clearly eligible Lightweight Change, repair first, verify fresh, and then recommend specific regression coverage. Never skip required verification or Human Gates."

policy:
  allow_implicit_invocation: true
